const appConfig = {
  API_PREFIX: `${import.meta.env.VITE_SERVER_API}/api`,
  AUTHENTICATED_ENTRY_PATH: "/dashboard",
  UN_AUTHENTICATED_ENTRY_PATH: "/sign-in",
  TOUR_PATH: "",
};

export default appConfig;